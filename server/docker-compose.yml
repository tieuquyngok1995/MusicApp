version: '3.9'
services:
  lesson-server:
    build: .
    container_name: lesson-server
    ports:
      - "3000:3000"
    volumes:
      - ./content:/app/content
      - ./data:/app/data
    restart: unless-stopped
